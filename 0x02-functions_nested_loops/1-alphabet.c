#include "main.h"

/**
* main - the main function
* more description
* Return: the return statement
*/

void print_alphabet(void)
{
	char alpha;
	for(alpha = 'a'; alpha <= 'z'; alpha++)
	{
		_putchar(alpha);
	}
	_putchar('\n');
	/* return (0); */
}
